# mattstack.dev

The front door: a static landing page that resolves the latest mattstack.app DMG
from m4ttstack/rt releases at view time. Deployed to Cloudflare Pages
(`bunx wrangler pages deploy . --project-name mattstack-dev`).
